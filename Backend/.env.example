#############################################
# Example environment variables for Backend
# Copy this file to ".env" and update values for your environment
#############################################

# Server
PORT=8080
NODE_ENV=development

# JWT / Auth
# Use a long, random string in production. Do NOT commit the real secret.
JWT_SECRET=your_jwt_secret_here

# Frontend URL (used for CORS / redirects)
FRONTEND_URL=http://localhost:5173

# MySQL / MariaDB connection (example local config)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=konecta
DB_USER=root
DB_PASSWORD=your_db_password_here

# Optional: change to true to enable verbose SQL logging
# DB_LOGGING=false

# Notes:
# - Remove the surrounding quotes from values when copying to a real .env
# - Keep secrets out of version control; use a secrets manager in production.
